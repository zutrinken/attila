<ul>
	{{#foreach navigation}}
		<li class="nav-{{slug}}{{#if current}} active{{/if}} umami--click--click-{{slug}}"><a href="{{url absolute="true"}}"><span>{{label}}</span></a></li>
	{{/foreach}}
</ul>
