<ul>
	{{#foreach navigation}}
		<li class="nav-{{slug}}{{#if current}} active{{/if}} "><a href="{{url absolute="true"}}" class="umami--click--click-{{slug}}><span>{{label}}</span></a></li>
	{{/foreach}}
</ul>
